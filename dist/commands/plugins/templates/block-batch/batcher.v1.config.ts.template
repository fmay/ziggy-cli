import { BlockGroupEnum, IFlowBlock, StyleTypeEnum } from '@ziggy/plugin-sdk'

export interface {{BLOCK_TYPE_PASCAL}}BlockV1Config {
  isBatch: boolean
  batchSize: number
  maxIterations: number
  // more properties here
}

export const {{BLOCK_TYPE_PASCAL}}V1Name = '{{BLOCK_TYPE}}'

export const Default{{BLOCK_TYPE_PASCAL}}BlockV1: IFlowBlock = {
  id: '',
  type: {{BLOCK_TYPE_PASCAL}}V1Name,
  width: 280,
  height: 140,
  position: { x: 0, y: 0 },
  data: {
    type: {{BLOCK_TYPE_PASCAL}}V1Name,
    name: '{{BLOCK_NAME}}',
    version: 1,
    isValid: true,
    group: BlockGroupEnum.Custom,
    styleType: StyleTypeEnum.standard,
    numInputs: 1,
    numOutputs: 1,
    hasInternalOutputHandles: false,
    hasInternalInputHandles: false,
    headerColor: '{{HEADER_COLOR}}',
    config: {
      isBatch: true,
      maxIterations: 0,
      batchSize: 10,
    } as {{BLOCK_TYPE_PASCAL}}BlockV1Config,
  },
}
